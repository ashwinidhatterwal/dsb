EFFICIENCY UPDATE

1. Replace code.gs in the existing Apps Script project. Preserve Script Properties. Save and deploy a New version of the EXISTING web-app deployment so the URL stays the same.
2. Keep the processTelegramQueue timer enabled. If background Telegram was never set up, run setupTelegramBackground once and authorize it. No extra timer is needed for this update. Old notification history migrates gradually; new orders enter the pending queue directly.
3. Upload the website files AND the updated .github/scripts files to your existing website repository. Keep .github/workflows/update-product-sitemap.yml. Do not upload the Android app.
4. Let GitHub Actions finish its sitemap and Pages deployment. The workflow uses one catalogue fetch to generate both the sitemap and product HTML. If the API fails, it stops rather than deploying empty pages.
5. Open the shop and refresh. Check the cart, sizes, admin pagination and a generated product link from the sitemap. Existing product.html?id= links continue to work.

Generated product pages live under products/ and are generated during deployment, not bundled as an outdated catalogue in this ZIP. New/changed product pages update when the workflow runs (daily, manual or after repository changes). Final price and stock remain server-verified at checkout.

Changes: admin timeouts and saved-versus-refresh feedback; 40-product admin pages; specific-order accounting lookup; pending-only Telegram delivery with bounded legacy migration; validated saved cart; browser Web Locks for supported browsers and idempotent cart cleanup; in-place review ratings; batched translation with dynamic text support; clearer long-wait checkout status; quieter homepage accents and aligned card content; removed 47 overridden CSS declarations; static product content and metadata; corrected outdated WhatsApp ordering copy.

Validation: 24 backend + 13 frontend regression scenarios passed. Admin response/timeout checks and an isolated full publish-preparation test also passed. Live timings and phone/browser visual checks could not be performed in this environment. Web Locks coordinate tabs where supported; other browsers retain the existing synchronous fallback.
